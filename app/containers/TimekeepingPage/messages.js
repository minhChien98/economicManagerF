/*
 * TimekeepingPage Messages
 *
 * This contains all the text for the TimekeepingPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TimekeepingPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TimekeepingPage container!',
  },
});
