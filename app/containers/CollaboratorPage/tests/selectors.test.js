// import { selectCollaboratorPageDomain } from '../selectors';

describe('selectCollaboratorPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
