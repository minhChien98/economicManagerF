/*
 * ExpensesPage Messages
 *
 * This contains all the text for the ExpensesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExpensesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExpensesPage container!',
  },
});
