/*
 * DialogAddCollaborators Messages
 *
 * This contains all the text for the DialogAddCollaborators component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DialogAddCollaborators';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DialogAddCollaborators component!',
  },
});
